import { env } from '../config/env.js';
export async function sendOtpEmail(email: string, code: string) { if (!env.RESEND_API_KEY || !env.EMAIL_FROM) throw Object.assign(new Error('Email service is not configured'), { status: 503, code: 'EMAIL_UNAVAILABLE' }); const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${env.RESEND_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({from:env.EMAIL_FROM,to:[email],subject:'Your HireUp AI sign-in code',text:`Your HireUp AI verification code is ${code}. It expires in 10 minutes.`})}); if(!response.ok) throw Object.assign(new Error('Email delivery failed'),{status:502,code:'EMAIL_DELIVERY_FAILED'}); }
